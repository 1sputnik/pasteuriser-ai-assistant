#include "DataReader.Tests.h"

TEST(Common_DataReader, OCDF_Read_Invalid_Data) {
	vector<OCDF> ocdfvec;
	//ocdfvec = read_OCDF_file();

	string first_intput = "../TestFiles/Invalid.csv";

	EXPECT_TRUE(true);
}
